save:
  basedir: 'experiments/matfold/log_kvrh'
  outdir: 'control'
  
data:
  data_loc: 'matbench_data/matbench_log_kvrh/matfold'
  folds: [0,1,2,3,4]
  target_name: 'log10(K_VRH)'
  test_size: 0.1
  batch_size: 879
  reweight: 'sqrt_inv'
  shuffle: true
  seed: 39
  cutoff: 5
  add_z_bond_coord: false
  atom_features: 'embed'
  edge_embed_size: 10

model:
  state_input_shape: 2

train:
  epoch_range: 500
  weighted_loss: false
  alpha_metric: 'dcor'
  dil_inform: false

hpo:
  trials: 10  # Number of Optuna trials (0 to skip)

multi_runs: 6  # Number of multi-seed runs after HPO

optimiser:
  name: 'AdamW'

scheduler:
  name: 'CosineAnnealingLR'
  
loss:
  loss: 'WeightedHuberLoss'
